#whitelist
branches:
    only:
        - develop

language: cpp

compiler: clang

before_install:
    - sudo apt-get install -qq libboost-dev

script: mkdir build && cd build && cmake .. && make && make test


